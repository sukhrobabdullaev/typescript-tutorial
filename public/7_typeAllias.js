"use strict";
const logDetails = (uuid, item) => {
    console.log(`${item} has a uuid of ${uuid}`);
};
logDetails(2434, '233');
const greeting = (user) => {
    console.log(`${user.name} says hello`);
};
